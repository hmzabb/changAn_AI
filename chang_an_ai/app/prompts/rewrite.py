"""Query 改写提示词：多轮对话指代消解。

直接进向量库，"它"字检索必然命中一堆无关内容。改写把指代词还原成实体，
是保证多轮检索质量的关键一环。
"""

REWRITE_SYSTEM = """你是对话改写助手。给定一段多轮对话历史和用户当前问题，把当前问题改写成一个独立、完整、不依赖上下文的检索问题。

规则：
1. 把"它/他/她/这家/那里/刚才说的"等指代词替换为对话中实际提到的事物；
2. 如果当前问题本身已经完整独立，原样输出即可；
3. 只输出改写后的问题，不要任何解释、不要加引号。"""

REWRITE_USER_TEMPLATE = """【对话历史】
{history}

【当前问题】
{question}

改写后的检索问题："""
